# yaml-language-server: $schema=https://json.schemastore.org/github-workflow.json
#
# Зеркало .github/workflows/ci.yml для внутреннего инстанса Gitea.
# Отличие от GitHub-версии одно — образ раннера в runs-on.

name: CI

on:
  push:
    branches: ['**']

env:
  NUGET_PACKAGES: ${{ github.workspace }}/.nuget/packages
  # SDK 10 умеет собирать net9.0, но dotnet test поднимает сборки каждого
  # таргета на своём рантайме, поэтому нужен и девятый.
  DOTNET_VERSION: |
    9.0.x
    10.0.x

jobs:
  build:
    # Метку node-24 раннер отображает на образ node:24-bookworm. Нода здесь
    # нужна не проекту, а раннеру: act_runner исполняет JavaScript-экшены
    # (checkout, cache, setup-dotnet) внутри контейнера задания системным node,
    # и в образе без него падает первый же шаг. .NET целиком ставит
    # setup-dotnet — как на GitHub-стороне. Подробности в CONTRIBUTING.md.
    runs-on: node-24

    steps:
      - name: Checkout code
        uses: actions/checkout@v7

      - name: Cache NuGet packages
        uses: actions/cache@v6
        with:
          path: ${{ env.NUGET_PACKAGES }}
          key: ${{ runner.os }}-nuget-${{ hashFiles('**/*.csproj', '**/Directory.Packages.props') }}
          restore-keys: |
            ${{ runner.os }}-nuget-

      - name: Setup .NET
        uses: actions/setup-dotnet@v6
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      - name: Test
        run: dotnet test --configuration Release --no-build

      - name: Pack (validate packaging)
        run: dotnet pack --configuration Release --no-build --output ./nupkg

      - name: Restore .NET tools
        run: dotnet tool restore

      # Только проверка: готовый сайт здесь никуда не выкладывается, Pages у
      # Gitea нет
      - name: Build documentation
        run: dotnet docfx docs/docfx.json
